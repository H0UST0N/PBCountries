import React from "react";
import { render } from '@testing-library/react-native';

import { Home } from '../../pages/Home';

test('Check if show correctly Home', () => {
    // const { debug } = render(<Home />)

    // debug();
});